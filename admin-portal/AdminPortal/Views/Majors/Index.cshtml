@model IEnumerable<AdminPortal.Models.Major>

@{
    ViewBag.Title = "UWYO Degree Programs";
}

<h2>UWYO Degree Programs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            Major
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bachelors)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Masters)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Doctorate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Other)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MajorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bachelors)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Masters)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Doctorate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Other)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}
</table>
